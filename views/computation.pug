extends layout

block content
  h1= title
  p Welcome to #{title}
  p Hi this is Mounika Annabathina and this is computation pug
  p Math.imul() is applied to #{number1} is #{imul}
  p Math.log() is applied to #{number2} is #{log}
  p Math.log10() is applied to #{number4} is #{log10}